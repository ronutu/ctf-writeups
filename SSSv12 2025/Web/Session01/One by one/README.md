# One by one

## Description

Get the flag from [one-by-one](http://141.85.224.70:8090/one-by-one/).

## Intuition

We see that refreshing the page shows us one character at a time from the flag.

## Solution

We create a script ([main.py](./main.py)) which concatenates all the characters.

## Flag

`SSS{this_is_a_very_long_flag_for_which_you_should_have_a_script_so_as_not_to_get_bored}`
